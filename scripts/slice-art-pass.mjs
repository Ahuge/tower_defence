// Placeholder — see scripts/slice_art_pass.py for the actual extractor.
// Kept here so the convention `scripts/<name>.mjs` doesn't get an
// orphan Python script invisible to the rest of the build.
console.log('Run: python3 scripts/slice_art_pass.py');
