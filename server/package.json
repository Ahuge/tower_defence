{
  "name": "td-signaling-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed — wrangler handles bundling'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.95.0"
  }
}
